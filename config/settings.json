{
  "year_range": {
    "start": 1990,
    "end": 2000,
    "comment": "10-year range for searches"
  },
  "delays": {
    "min_seconds": 1.0,
    "max_seconds": 2.0,
    "comment": "OPTIMIZED: Balanced to avoid rate limiting (faster than old 2-5s, safer than new 0.1-0.2s)"
  },
  "pause_every_n": 100,
  "pause_duration": 10,
  "comment_pause": "Pause every 100 searches for 10 seconds to avoid detection",
  "browser": {
    "headless": true,
    "comment": "Headless mode for VPS (no GUI needed)"
  },
  "num_processes": 4,
  "comment_workers": "4 threads for 4 vCPU VPS",
  "use_multiprocessing": true,
  "checkpoint_interval": 1000,
  "comment_checkpoint": "Save checkpoint every 1000 searches",
  "batch_size": 100,
  "comment_batch": "Write results in batches of 100 matches",
  "batch_timeout": 300,
  "output_dir": "./data/results",
  "input_dir": "./data",
  "checkpoint_dir": "./checkpoints",
  "performance_settings": {
    "max_retries": 2,
    "timeout_seconds": 20,
    "page_load_timeout": 120,
    "comment": "Increased timeouts for slow website"
  }
}
