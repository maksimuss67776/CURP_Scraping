{
  "year_range": {
    "start": "1977-12",
    "end": "1977-12",
    "comment": "Supports both year (1990) and year-month (1990-11) formats. For year-month, all combinations between start and end will be generated (e.g., 1990-11 to 2000-12)"
  },
  "delays": {
    "min_seconds": 0.7,
    "max_seconds": 1.5,
    "comment": "FAST MODE: Reduced delays for faster processing"
  },
  "pause_every_n": 150,
  "pause_duration": 8,
  "comment_pause": "Pause every 150 searches for 8 seconds to avoid detection",
  "browser": {
    "headless": true,
    "comment": "Headless mode for VPS (no GUI needed)"
  },
  "num_processes": 4,
  "comment_workers": "4 workers recommended - too many workers (10+) causes rate limiting and missed results",
  "use_multiprocessing": true,
  "checkpoint_interval": 1000,
  "comment_checkpoint": "Save checkpoint every 1000 searches",
  "batch_size": 100,
  "comment_batch": "Write results in batches of 100 matches",
  "batch_timeout": 300,
  "output_dir": "./data/results",
  "input_dir": "./data",
  "checkpoint_dir": "./checkpoints",
  "performance_settings": {
    "max_retries": 2,
    "timeout_seconds": 20,
    "page_load_timeout": 120,
    "comment": "Increased timeouts for slow website"
  }
}